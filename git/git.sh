#! /bin/bash

# Install Git
brew install git

# Install Git LFS
brew install git-lfs

# Configure Git
git config --global user.name "Brandon Mailhiot"
git config --global user.email "brandon.mailhiot@gmail.com"
git config --global core.editor "code --wait"